<Window x:Class="Problem2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Problem2"
        mc:Ignorable="d"
        Title="MainWindow" Height="282" Width="574">
    <Window.Resources>
        <local:Double2TextConverter x:Key="DataConverter"/>

        <Style TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"></Setter>
            <Setter Property="FontSize" Value="15"></Setter>
            <Setter Property="Margin" Value="0,10"></Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="FontWeight" Value="Bold"></Setter>
            <Setter Property="FontSize" Value="15"></Setter>
            <Setter Property="Padding" Value="2"></Setter>
            <Setter Property="Margin" Value="0,10"></Setter>
        </Style>

        <Style TargetType="Slider">
            <Setter Property="Margin" Value="0,10"></Setter>
            <Setter Property="FontWeight" Value="Bold"></Setter>
            <Setter Property="MaxHeight" Value="100"></Setter>
        </Style>
    </Window.Resources>
    <Border Margin="15">
        <StackPanel>
            

            <!--Bind via XAML-->
            <TextBlock Text="Bind via XAML"></TextBlock>
            <TextBox x:Name="txtXAML" Text="{Binding Path=Value, ElementName=slrData, Converter={StaticResource DataConverter},
                                                UpdateSourceTrigger=PropertyChanged}"></TextBox>
            
            <!--Bind via code-->
            <TextBlock Text="Bind via code"></TextBlock>
            <TextBox x:Name="txtCode"></TextBox>

            <Slider x:Name="slrData"></Slider>
        </StackPanel>
    </Border>
</Window>
