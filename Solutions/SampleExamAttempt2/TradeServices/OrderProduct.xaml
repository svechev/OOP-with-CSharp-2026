<UserControl x:Class="TradeServices.OrderProduct"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TradeServices"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500" Background="White">
    <UserControl.Resources>
        <Style TargetType="Label">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="25"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="30"/>
            <Setter Property="FontSize" Value="25"/>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <!--labels-->
        <Label Content="Product" Grid.Row="0" Grid.Column="0"></Label>
        <Label Content="Quantity" Grid.Row="1" Grid.Column="0"></Label>
        
        <!--product combo box-->
        <ComboBox x:Name="cboProduct" FontSize="25" Grid.Row="0" Grid.Column="1" Margin="30"></ComboBox>
        
        <!--textbox quantity-->
        <TextBox x:Name="txtQty" Margin="30" Grid.Row="1" Grid.Column="1"
                 Padding="2" FontSize="25" Text="0"></TextBox>
        
        <!--buttons-->
        <Button x:Name="btnOrder" Content="Order" Grid.Row="2" Grid.Column="0" Click="btnOrder_Click"></Button>
        <Button x:Name="btnCancel" Content="Cancel" Grid.Row="2" Grid.Column="1" Click="btnCancel_Click"></Button>
        
    </Grid>
</UserControl>
