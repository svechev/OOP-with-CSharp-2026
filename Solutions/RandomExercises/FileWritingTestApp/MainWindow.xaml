<Window x:Class="FileWritingTestApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileWritingTestApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="600" Closing="Window_Closing">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="HorizontalAlignment" Value="Right"></Setter>
            <Setter Property="VerticalAlignment" Value="Center"></Setter>
            <Setter Property="FontSize" Value="25"></Setter>
            <Setter Property="FontWeight" Value="Bold"></Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="25"></Setter>
            <Setter Property="VerticalAlignment" Value="Center"></Setter>
            <Setter Property="HorizontalAlignment" Value="Center"></Setter>
            <Setter Property="Width" Value="200"></Setter>
            <Setter Property="Padding" Value="2"></Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="25"></Setter>
            <Setter Property="VerticalAlignment" Value="Center"></Setter>
            <Setter Property="HorizontalAlignment" Value="Center"></Setter>
            <Setter Property="Width" Value="150"></Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20, 5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition> 
            <ColumnDefinition Width="*"></ColumnDefinition> 
        </Grid.ColumnDefinitions>

        <!--name-->
        <Label x:Name="lblName" Content="Name:" Grid.Column="0" Grid.Row="0"></Label>
        <TextBox x:Name="txtName" Grid.Row="0" Grid.Column="1"></TextBox>
        
        <!--age-->
        <Label x:Name="lblAge" Content="Age:" Grid.Column="0" Grid.Row="1"></Label>
        <TextBox x:Name="txtAge" Grid.Row="1" Grid.Column="1"></TextBox>
        
        <!--buttons-->
        <Button x:Name="btnSubmit" Content="Submit" Grid.Row="2" Grid.Column="0" Click="btnSubmit_Click"></Button>
        <Button x:Name="btnClear" Content="Clear" Grid.Row="2" Grid.Column="1" Click="btnClear_Click"></Button>
    </Grid>
</Window>
