<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AlarmClockApp"
        xmlns:ClockLib="clr-namespace:ClockLib;assembly=ClockLib" x:Class="AlarmClockApp.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="800">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!--left column-->
        <StackPanel Grid.Column="0">
            <ClockLib:DigitalClock  VerticalAlignment="Top" ClockStarted="DigitalClock_ClockStarted" TimeUpdated="DigitalClock_TimeUpdated"/>
            <Button x:Name="btnDistinct" Content="Find distinct numbers" Margin="10" FontSize="25" Click="btnDistinct_Click"></Button>
            <TextBox x:Name="txtDistinct" 
                     VerticalScrollBarVisibility="Auto" 
                     AcceptsTab="True"
                     TextWrapping="Wrap" AcceptsReturn="True" Height="140"></TextBox>
        </StackPanel>

        <!--right column-->
        <StackPanel Grid.Column="1">

            <!--ring after-->
            <StackPanel Orientation="Horizontal">
                <Label Content="Ring after[min]:"></Label>
                <TextBox x:Name="txtRing" Width="55" Text="1.0"></TextBox>
            </StackPanel>

            <!--slider-->
            <Slider x:Name="slrRing" Minimum="0" Maximum="5" Margin="5" TickPlacement="BottomRight"></Slider>

            <!--label beep indicator-->
            <Label x:Name="lblBeep" Content="Beep indicator:"></Label>
            <!--the large textbox-->
            <TextBox x:Name="txtIndicator" TextWrapping="Wrap" AcceptsReturn="True" Height="340"></TextBox>
        </StackPanel>
    </Grid>
</Window>
